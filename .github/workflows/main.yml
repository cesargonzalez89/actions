name: Test

on: [push]

jobs: 
    test-build:
        runs-on: ubuntu-latest

        steps: 
            - name: ls
              run: |
                echo "HolA Mundo" >> hola.txt
                cat hola.txt